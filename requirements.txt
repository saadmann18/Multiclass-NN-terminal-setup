# Production dependencies for MNIST CNN project
# These are automatically managed by pyproject.toml
# This file is kept for Docker builds and legacy compatibility

# Install the package with all dependencies:
# pip install -e .

torch>=2.0.0
torchvision>=0.15.0
tqdm>=4.65.0
matplotlib>=3.7.0
PyYAML>=6.0
